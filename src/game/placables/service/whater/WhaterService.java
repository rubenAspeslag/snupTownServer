package game.placables.service.whater;

import game.placables.service.Service;

public class WhaterService extends Service {
    public WhaterService(int width, int length, int serviceRadius, int capasety, int price, int selPrice) {
        super(width, length, serviceRadius, capasety, price, selPrice, false);
    }
}
